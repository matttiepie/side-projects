services:
  db:
    image: postgres:16
    container_name: demoapi-db
    environment:
      POSTGRES_DB: demoapi
      POSTGRES_USER: demo
      POSTGRES_PASSWORD: demo
    ports:
      - "5432:5432"
    volumes:
      - demoapi_pgdata:/var/lib/postgresql/data

volumes:
  demoapi_pgdata: